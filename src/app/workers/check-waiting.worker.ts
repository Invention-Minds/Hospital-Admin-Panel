// // / <reference lib="webworker" />

// // Web Worker: Runs independently from the main UI thread
// setInterval(() => {
//     postMessage('runCheckWaitingTimes'); // Sends a message to the component every 1 min
//   }, 60000); // Run every 60 seconds